package enums;

/**
 * StudentSpecs ENUM to get StudentAPI related data from Config Properties file
 */
public enum StudentSpecs implements ConfigProperties{

    STUDENTBASEURI,
    STUDENTPORT,
    STUDENTAPIENDPOINT
}
